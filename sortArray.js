var nextIndex = index+1;
var holder = 0;

function sort(arr){
 For(index=0; index < x.length; index++){
 if(arr[index] > arr[nextIndex]){
    holder = arr[index];
	arr[nextIndex] = holder;
	}
	
  
  }
  console.log(x);
}

